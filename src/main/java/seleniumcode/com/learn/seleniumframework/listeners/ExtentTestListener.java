package seleniumcode.com.learn.seleniumframework.listeners;

/**
 * Placeholder listener class in main sources.
 *
 * Note: The actual TestNG listener implementation (which depends on TestNG) is placed under
 * `src/test/java/.../listeners/ExtentTestListener.java` because TestNG is declared with
 * test scope in the project's `pom.xml`.
 *
 * Keeping this lightweight placeholder prevents compilation errors for main-scope code that
 * may reference the listener package while avoiding a hard dependency on TestNG at runtime.
 */
public class ExtentTestListener {
    // Intentionally empty - see src/test/java/.../listeners/ExtentTestListener.java
    public static void noop() {
        // no-op
    }
}
